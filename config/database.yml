default: &default
  adapter: postgresql
  encoding: unicode
  host: <%= ENV["DB_IP"] %> 
  username: <%= ENV["DB_USER"] %>
  password: <%= ENV["DB_PASS"] %>
  pool: 5

development:
  <<: *default
  database: emp_attend

production:
  <<: *default
  database: emp_attend

test:
  <<: *default
  database: emp_attend_test